define([
        'angular',
        'app/js/services/services'
        
        ], function(angular){
	var filters = angular.module('app.filters', ['app.services']);
	
	//add filters here
	
	return filters;
});